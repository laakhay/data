# Kraken

Exports:

- `KrakenProvider`
- `KrakenRESTConnector`
- `KrakenWSConnector`
- `KrakenURM`

Current registered market types: `spot`, `futures`.

Notable current differences:

- `funding_rates` currently discovered as REST-only.
- `historical_trades` appears as REST-supported.
