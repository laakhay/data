# Core API

Core modules define all shared contracts used by API, router, providers, and models.

- `enums`: canonical market/data enums and `InstrumentSpec`.
- `exceptions`: structured error hierarchy.
- `urm`: symbol mapping protocol and registry.
- `router-registry`: runtime dispatch and provider lifecycle.
