# Models

All models are immutable (frozen) pydantic/dataclass structures.

Categories:

- market data (`Bar`, `OHLCV`, `OrderBook`, `Trade`)
- derivatives (`FundingRate`, `OpenInterest`, `Liquidation`, `MarkPrice`)
- metadata/events (`Symbol`, `SeriesMeta`, `StreamingBar`, `DataEvent`, `ConnectionEvent`)
